.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/

.env
.env.*

.data/
.git/
.gitignore
.dockerignore

.claude/
.idea/
.vscode/

node_modules/
web/node_modules/

step_archive/

*.md
README*

# data/ 중 라이브에 안 필요한 큰 파일들 — JSON만 가게
data/unihan/
data/app.db

# Obsidian Vault — 인간 지식 저장소, 운영 코드 무관
vault/

# 테스트 파일 — Railway 운영 컨테이너에 불필요 (CI에서만 사용)
**/test_*.py
**/conftest.py
**/pytest.ini
.pytest_cache/

# 디버그 잔재
tarot_*.png
tarot_*.json

# 외부 보고서 원본 (.gitignore와 정합)
/Export.md
/[가-힣]*보고서*.md
/[가-힣]*분석*.md

# GitHub Actions / CI 설정 (Railway 빌드와 무관)
.github/

# ─── 빌드 컨텍스트 슬림화 (2026-06-21) ───
# 루트 검증 스크래치 (_verify_·_webtoon_ 등) — 런타임 무관
/_*
/bootstrap-claude-commands.sh
# 백업 바이너리 (원본 de440s.bsp·unet_weights 는 유지)
*_backup.pt
*.local-backup
# SOTA 학습 앙상블·학습 부산물 — 런타임은 unet_weights{.pt,_cfm.onnx} 만 로드
models/sota/
models/*_30ep.*
models/*_60ep.*
# Unihan 원본 zip+txt — 빌드 결과 JSON만 필요
assets/unihan/Unihan.zip
assets/unihan/*.txt
# 디버그·학습 스크립트 (운영 컨테이너 불필요)
scripts/playwright_*.py
scripts/demo_*.py
scripts/debug_*.py
scripts/sota_phase*.py
